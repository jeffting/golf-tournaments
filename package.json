{
  "name": "golf-tourney-tracker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:dev": "cp .env.development .env.production.local && next build && rm .env.production.local",
    "deploy": "npm run build && firebase deploy --only hosting",
    "deploy:dev": "firebase use default && npm run build:dev && firebase deploy --only hosting",
    "deploy:prod": "firebase use production && npm run build && firebase deploy --only hosting",
    "deploy:rules": "firebase deploy --only firestore:rules",
    "deploy:rules:dev": "firebase use default && firebase deploy --only firestore:rules",
    "deploy:rules:prod": "firebase use production && firebase deploy --only firestore:rules",
    "deploy:storage:dev": "firebase use default && firebase deploy --only storage",
    "deploy:storage:prod": "firebase use production && firebase deploy --only storage",
    "deploy:indexes:dev": "firebase use default && firebase deploy --only firestore:indexes",
    "deploy:indexes:prod": "firebase use production && firebase deploy --only firestore:indexes",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "@dataconnect/generated": "file:src/dataconnect-generated",
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1",
    "@mui/icons-material": "^7.3.7",
    "@mui/material": "^7.3.6",
    "dotenv": "^17.3.1",
    "firebase": "^12.10.0",
    "next": "16.1.1",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.1.1",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}